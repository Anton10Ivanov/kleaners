
import { memo } from "react";
import { motion } from "framer-motion";

export const HeroImage = memo(() => {
  // Component removed as its functionality has been integrated directly into BackgroundElements.tsx
  // This is an empty component now that can be safely removed if desired
  return null;
});

HeroImage.displayName = "HeroImage";
